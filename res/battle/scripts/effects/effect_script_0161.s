    .include "macros/btlcmd.inc"

    .data

_000:
    CompareMonDataToValue OPCODE_EQU, BTLSCR_ATTACKER, BATTLEMON_STOCKPILE_COUNT, 0, _064
    UpdateMonDataFromVar OPCODE_GET, BTLSCR_ATTACKER, BATTLEMON_STOCKPILE_COUNT, BTLVAR_MOVE_POWER
    UpdateVar OPCODE_MUL, BTLVAR_MOVE_POWER, 120
	
    UpdateMonData OPCODE_SUB_TO_ZERO, BTLSCR_ATTACKER, BATTLEMON_STOCKPILE_COUNT, 2
	UpdateMonData OPCODE_SUB_TO_ZERO, BTLSCR_ATTACKER, BATTLEMON_STOCKPILE_DEF_BOOSTS, 2
    UpdateMonData OPCODE_SUB_TO_ZERO, BTLSCR_ATTACKER, BATTLEMON_STOCKPILE_SPDEF_BOOSTS, 2
	
	CalcCrit 
    CalcMaxDamage
	
    UpdateVar OPCODE_FLAG_ON, BTLVAR_BATTLE_CTX_STATUS, SYSCTL_MESSAGE_AFTER_MISS
    UpdateVar OPCODE_SET, BTLVAR_SIDE_EFFECT_FLAGS_DIRECT, MOVE_SIDE_EFFECT_TO_ATTACKER|MOVE_SUBSCRIPT_PTR_SPIT_UP
    End 

_064:
    PrintAttackMessage 
    Wait 
    WaitButtonABTime 30
    // But it failed to spit up a thing!
    PrintMessage pl_msg_00000368_00814, TAG_NONE
    Wait 
    WaitButtonABTime 30
    UpdateVar OPCODE_FLAG_ON, BTLVAR_MOVE_STATUS_FLAGS, MOVE_STATUS_NO_MORE_WORK
    End 
